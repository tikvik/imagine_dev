extends layout.pug
block body
	body.people
block content
	main#content
		each proj, index in projects

			if proj.project[0].category == "articles" 
				include partials/article.pug
		section.read-more
			a(href="articles.html") Read more from articles
		each proj, index in projects

			if proj.project[0].category == "interviews" 
				include partials/article.pug
		section.read-more
			a(href="interviews.html") Read more from interviews


